/**
 * 路由拦截器
 */
export const beforeEachFn = (to, from, next) => {
  // const token = router.app.$storage.fetch('token')
  // const needAuth = to.matched.some(item => item.meta.login)
  // if (!token && needAuth) return next({ path: '/login' })
  // next()
}
